# Patching Rules

When output is poor, patch in this order:

1. Lock output format
2. Add guardrails
3. Add delimiters
4. Add checklists
5. Add stop conditions

Never patch randomly.
Each patch must address a specific failure.
